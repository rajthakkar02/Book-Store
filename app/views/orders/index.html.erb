<p style="color: green"><%= notice %></p>
<h1>Orders</h1>
<div id="orders">
  <ol>
    <% @orders.each do |order| %>
    <li>
      <%= render order %>
      <p>
        <%= link_to "Show this order", order if current_user.seller? %>
      </p>
      </li>
    <% end %>
  </ol>
</div>
<%= link_to "New order", new_order_path if current_user.seller?%>
<br>
<%= link_to "Dashboard", root_path%>
